import { CheckCircle, Star, Clock, Calendar, User, Award, Code, Database, Server, Globe, ArrowRight, Users } from 'lucide-react';
import { Link } from 'react-router-dom';
import CallToAction from '../../components/CallToAction';
import FAQ from '../../components/FAQ';

// Component implementation similar to MernStack.tsx but with DevOps-specific content
// Full implementation omitted for brevity

const DevOps = () => {
  return (
    <div>
      {/* Header */}
      <section className="pt-32 pb-20 bg-gradient-to-r from-orange-700 to-orange-600 text-white">
        {/* Content */}
      </section>
      
      {/* Other sections */}
    </div>
  );
};

export default DevOps;