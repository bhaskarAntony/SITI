import { CheckCircle, Star, Clock, Calendar, User, Award, Code, Database, Server, Globe, ArrowRight, Users } from 'lucide-react';
import { Link } from 'react-router-dom';
import CallToAction from '../../components/CallToAction';
import FAQ from '../../components/FAQ';

// Component implementation similar to MernStack.tsx but with Cloud Computing-specific content
// Full implementation omitted for brevity

const CloudComputing = () => {
  return (
    <div>
      {/* Header */}
      <section className="pt-32 pb-20 bg-gradient-to-r from-cyan-700 to-cyan-600 text-white">
        {/* Content */}
      </section>
      
      {/* Other sections */}
    </div>
  );
};

export default CloudComputing;